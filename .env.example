GEMINI_API_KEY=

# Optional second key from a DIFFERENT Google account/project. Free-tier quota is
# per project, so a second key doubles requests/minute (5 -> 10). Leave blank to use one.
GEMINI_API_KEY_2=
